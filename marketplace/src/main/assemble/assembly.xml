<project name="diff-assembly" default="build" xmlns:nx="urn:nuxeo-build" xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml" uri="urn:nuxeo-artifact" />

  <target name="build" description="Build Nuxeo Diff Marketplace package">
    <tstamp />
    <delete failonerror="false" dir="${maven.project.build.directory}/marketplace" />
    <mkdir dir="${maven.project.build.directory}/marketplace" />
    <copy todir="${maven.project.build.directory}/marketplace">
      <fileset dir="src/main/resources" />
      <filterset>
        <filter token="VERSION" value="${marketplace.diff.version}" />
        <filter token="DISTRIB_VERSION" value="${nuxeo.distribution.version}" />
      </filterset>
    </copy>
    <copy todir="${maven.project.build.directory}">
      <artifact:file key="org.nuxeo.ecm.distribution:nuxeo-nxr-server::zip" />
      <artifact:file key="org.nuxeo.ecm.distribution:nuxeo-nxr-jsf-ui::zip" />
      <artifact:file key="org.nuxeo.marketplace.diff:ear::zip" />
    </copy>
    <unzip src="${maven.project.build.directory}/nuxeo-nxr-server-${nuxeo.distribution.version}.zip"
           dest="${maven.project.build.directory}/nuxeo-nxr-server-jsf-ui" />
    <unzip src="${maven.project.build.directory}/nuxeo-nxr-jsf-ui-${nuxeo.distribution.version}.zip"
           dest="${maven.project.build.directory}/nuxeo-nxr-server-jsf-ui" />
    <zip destfile="${maven.project.build.directory}/nuxeo-nxr-server-jsf-ui-${nuxeo.distribution.version}.zip"
         basedir="${maven.project.build.directory}/nuxeo-nxr-server-jsf-ui" />
    <!-- Diff against Nuxeo Server + JSF UI -->
    <nx:zipdiff file1="${maven.project.build.directory}/ear-${marketplace.diff.version}.zip"
                file2="${maven.project.build.directory}/nuxeo-nxr-server-${nuxeo.distribution.version}.zip"
                includesfile="${maven.project.build.directory}/includes"
                excludesfile="${maven.project.build.directory}/excludesfile"
                ignoreContent="true"
                ignoreContentPattern=".*SNAPSHOT\\.jar$"
                patternsetid="diff.versus.jsf">
    </nx:zipdiff>
    <unzip src="${maven.project.build.directory}/ear-${marketplace.diff.version}.zip"
           dest="${maven.project.build.directory}/marketplace/install">
      <patternset refid="diff.versus.jsf" />
    </unzip>

    <zip destfile="${maven.project.build.directory}/${maven.project.artifactId}-${marketplace.diff.version}.zip"
         basedir="${maven.project.build.directory}/marketplace" />
    <artifact:attach file="${maven.project.build.directory}/${maven.project.artifactId}-${marketplace.diff.version}.zip"
                     type="zip" />

    <!-- cleanups -->
    <delete dir="${outdir}/marketplace" failonerror="false" />
    <delete dir="${outdir}/nuxeo-nxr-server-jsf-ui" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-server-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-jsf-ui-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nuxeo-nxr-server-jsf-ui-${nuxeo.distribution.version}.zip" failonerror="false" />
    <delete file="${outdir}/nxr-${maven.project.version}.zip" failonerror="false" />
    <delete file="${outdir}/ear-${marketplace.diff.version}.zip" failonerror="false" />
  </target>

</project>
